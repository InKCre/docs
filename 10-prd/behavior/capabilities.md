# Capabilities

## Capability: Collection

- related claim(s): collect external information into reusable units
- non-goal: collection is not an information state, and neither source configuration nor a source-native object store becomes the product's shared memory

## Capability: Info-base organization

- related claim(s): organize information in one reusable info-base
- non-goal: organization does not own collection-time graph construction or application indexes; breakdown, merge, and linking are known operations rather than a complete list

## Capability: Retrieval and downstream use

- related claim(s): expose info-base information for retrieval and downstream use
- non-goal: application indexes, embeddings, and projections do not take over source or info-base authority

## Capability: Semantic retrieval

- related claim(s): expose info-base information for retrieval and downstream use; retrieve semantically related info-base entities
- non-goal: semantic retrieval does not generate an answer, create transient information units, silently maintain its index while reading, or subsume feature and graph-navigation retrieval

## Capability: Lexical feature retrieval

- related claim(s): expose info-base information for retrieval and downstream use; recall blocks from lexical feature evidence
- non-goal: lexical feature retrieval does not generate an answer, recursively index the graph, replace semantic or graph-navigation retrieval, or imply perceptual/hybrid retrieval is implemented

## Capability: Media interpretation

- related claim(s): organize information in one reusable info-base; recall blocks from lexical feature evidence
- non-goal: system-driven interpretation is one additive organization approach; it does not replace faithful Resolver materialization, write retrieval records directly, or define all media organization

## Capability: Focal-block rumination

- related claim(s): organize information in one reusable info-base; retrieve semantically related info-base entities
- non-goal: rumination is one organization approach rather than the definition of organization; it does not replace or delete the focal block, promise a mutation, or run automatically during collection

## Capability: Extension-based capability growth

- related claim(s): collect external information into reusable units; expose info-base information for retrieval and downstream use; accept memo-like capture through familiar clients
- non-goal: extensions may add protocol APIs and compose their authentication, but do not bypass graph authority or silently inherit a core terminal-user model

## Capability: Memo-like capture

- related claim(s): accept memo-like capture through familiar clients
- non-goal: a memo-compatible backend does not become a complete clone of the external product and does not include browsing or using the wider InKCre info-base

## Capability: Multi-runtime peer participation

- related claim(s): organize information in one reusable info-base
- non-goal: one runtime does not become the only valid participant in the shared system
